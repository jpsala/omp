diff --git a/packages/coding-agent/src/config/settings-schema.ts b/packages/coding-agent/src/config/settings-schema.ts
index bdad59976afca64954e44d1f1e8d893d8b4ec2ac..f4a385296fef216e7270894b40d59473e8c37d00 100644
--- a/packages/coding-agent/src/config/settings-schema.ts
+++ b/packages/coding-agent/src/config/settings-schema.ts
@@ -1306,6 +1306,33 @@ export const SETTINGS_SCHEMA = {
 		},
 	},
 
+	"display.hiddenTools": {
+		type: "array",
+		default: [] as string[],
+		ui: {
+			tab: "appearance",
+			group: "Display",
+			label: "Hidden Tools",
+			description: "Tool names whose calls and results are hidden from the transcript",
+		},
+	},
+
+	"display.hideAssistantToolPreambles": {
+		type: "boolean",
+		default: false,
+		ui: {
+			tab: "appearance",
+			group: "Display",
+			label: "Hide Assistant Tool Preambles",
+			description: "Hide assistant text emitted immediately before a tool call",
+		},
+	},
+
+	"display.transcriptVisibilityProfiles": {
+		type: "record",
+		default: {} as Record<string, unknown>,
+	},
+
 	"display.showTokenUsage": {
 		type: "boolean",
 		default: false,
diff --git a/packages/coding-agent/src/extensibility/extensions/types.ts b/packages/coding-agent/src/extensibility/extensions/types.ts
index e79897301e8aa5d3a6b4eb8922bfd5b3856c6b96..6c7aedc32408d40c945000bd5650988daa7e4f00 100644
--- a/packages/coding-agent/src/extensibility/extensions/types.ts
+++ b/packages/coding-agent/src/extensibility/extensions/types.ts
@@ -253,6 +253,14 @@ export type AutocompleteProviderFactory = (current: AutocompleteProvider) => Aut
 // (custom editor component, header/footer, widgets, theming, terminal input)
 // and may be invoked from event handlers that have already taken the agent
 // loop's lock — hooks intentionally cannot.
+export interface TranscriptVisibility {
+	hideThinking: boolean;
+	hideToolActivity: boolean;
+	hideAssistantToolPreambles: boolean;
+	hideTokenUsage: boolean;
+	hiddenTools: string[];
+}
+
 export interface ExtensionUIContext {
 	/** True when selector timeouts start only after the dialog is presented. */
 	timeoutStartsOnPresentation?: boolean;
@@ -368,6 +376,18 @@ export interface ExtensionUIContext {
 
 	/** Set tool output expansion state. */
 	setToolsExpanded(expanded: boolean): void;
+
+	/** Read transcript visibility preferences when the interactive TUI exposes them. */
+	getTranscriptVisibility?(): TranscriptVisibility;
+
+	/** Persist transcript visibility preferences and rebuild the native transcript in place. */
+	setTranscriptVisibility?(visibility: TranscriptVisibility): void;
+
+	/** Read globally persisted named transcript-visibility profiles. */
+	getTranscriptVisibilityProfiles?(): Record<string, TranscriptVisibility>;
+
+	/** Replace the globally persisted named transcript-visibility profiles. */
+	setTranscriptVisibilityProfiles?(profiles: Record<string, TranscriptVisibility>): void;
 }
 
 /** Visual composer style and selector copy registered by an extension. */
diff --git a/packages/coding-agent/src/modes/components/chat-transcript-builder.ts b/packages/coding-agent/src/modes/components/chat-transcript-builder.ts
index 7995efb4129d4528cc164a11b8d96c1e4abcc44c..ccc22e4810006ad03b8800618c3f86844dca2e57 100644
--- a/packages/coding-agent/src/modes/components/chat-transcript-builder.ts
+++ b/packages/coding-agent/src/modes/components/chat-transcript-builder.ts
@@ -105,6 +105,7 @@ export class ChatTranscriptBuilder {
 
 	constructor(private readonly deps: ChatTranscriptBuilderDeps) {
 		this.container.setToolActivityVisible(!settings.get("display.hideToolActivity"));
+		this.container.setHiddenToolActivityNames(settings.get("display.hiddenTools"));
 	}
 
 	/** Whether the transcript currently holds any rendered rows. */
@@ -115,6 +116,7 @@ export class ChatTranscriptBuilder {
 	/** Discard all components and rebuild the whole transcript from `entries`. */
 	rebuild(entries: SessionMessageEntry[]): void {
 		this.reset();
+		this.container.setHiddenToolActivityNames(settings.get("display.hiddenTools"));
 		for (const entry of entries) this.#appendEntry(entry);
 		// Flush the trailing turn's usage row only once its tools are materialized
 		// (a read whose result has not arrived stays pending); otherwise the row
@@ -376,7 +378,9 @@ export class ChatTranscriptBuilder {
 	#appendAssistantMessage(message: Extract<AgentMessage, { role: "assistant" }>): void {
 		const hideThinkingBlock = this.deps.hideThinkingBlock?.() ?? false;
 		const proseOnlyThinking = this.deps.proseOnlyThinking ? this.deps.proseOnlyThinking() : true;
-		const timeline = splitAssistantMessageToolTimeline(message);
+		const timeline = splitAssistantMessageToolTimeline(message, {
+			hideToolPreamble: settings.get("display.hideAssistantToolPreambles"),
+		});
 		const assistantComponent = new AssistantMessageComponent(
 			timeline.beforeTools,
 			hideThinkingBlock,
diff --git a/packages/coding-agent/src/modes/components/read-tool-group.ts b/packages/coding-agent/src/modes/components/read-tool-group.ts
index 8ff97e63ac38e9aa0f2a8db6f69bbb3495d95756..87f3d6da3157b75a64ea5fee2702e6f4e155f31d 100644
--- a/packages/coding-agent/src/modes/components/read-tool-group.ts
+++ b/packages/coding-agent/src/modes/components/read-tool-group.ts
@@ -530,6 +530,10 @@ export class ReadToolGroupComponent extends Container implements ToolExecutionHa
 		super.invalidate();
 	}
 
+	getToolActivityName(): string {
+		return "read";
+	}
+
 	getComponent(): Component {
 		return this;
 	}
diff --git a/packages/coding-agent/src/modes/components/tool-activity.ts b/packages/coding-agent/src/modes/components/tool-activity.ts
index 5772a9b7fdc4b7c8f6844f97b0b3c9b15a8490f4..646661f7e62d210d561699bf7d71f181417b683b 100644
--- a/packages/coding-agent/src/modes/components/tool-activity.ts
+++ b/packages/coding-agent/src/modes/components/tool-activity.ts
@@ -2,6 +2,8 @@ import { type Component, Container } from "@oh-my-pi/pi-tui";
 
 export interface ToolActivityComponent {
 	setToolActivityVisible(visible: boolean): void;
+	/** Built-in or extension tool name for per-tool transcript filtering. */
+	getToolActivityName?(): string;
 }
 
 export function isToolActivityComponent(component: Component): component is Component & ToolActivityComponent {
diff --git a/packages/coding-agent/src/modes/components/tool-execution.ts b/packages/coding-agent/src/modes/components/tool-execution.ts
index 910bbd3771552595131070e1ae96b4ece5256f9a..eb8e3ebf61e04768b08a2e0dd730593279f51be9 100644
--- a/packages/coding-agent/src/modes/components/tool-execution.ts
+++ b/packages/coding-agent/src/modes/components/tool-execution.ts
@@ -787,6 +787,10 @@ export class ToolExecutionComponent extends Container {
 		super.invalidate();
 	}
 
+	getToolActivityName(): string {
+		return this.#toolName;
+	}
+
 	setShowImages(show: boolean): void {
 		this.#showImages = show;
 		this.#updateDisplay();
diff --git a/packages/coding-agent/src/modes/components/transcript-container.ts b/packages/coding-agent/src/modes/components/transcript-container.ts
index 0a718a18e130bb6544aa7c60b37651adea36a7f4..f42f316ef40aebc7f8e001bf83dcddc094671370 100644
--- a/packages/coding-agent/src/modes/components/transcript-container.ts
+++ b/packages/coding-agent/src/modes/components/transcript-container.ts
@@ -142,6 +142,7 @@ export class TranscriptContainer extends Container {
 	#replayPending = false;
 	#replayRequested = false;
 	#toolActivityVisible = true;
+	#hiddenToolActivityNames = new Set<string>();
 	#lastFrame: AnimationFrame = { tick: 0, now: 0 };
 	// Start rows from the last full render(), keyed by child component (transcript deep-links).
 	#childStartRows = new Map<Component, number>();
@@ -150,8 +151,16 @@ export class TranscriptContainer extends Container {
 	// allocations. Logs once per pinned episode after a grace period.
 	#pinnedFrontier: { index: number; since: number; logged: boolean } | undefined;
 
+	#applyToolActivityVisibility(component: Component): void {
+		if (!isToolActivityComponent(component)) return;
+		const toolName = component.getToolActivityName?.().trim().toLowerCase();
+		component.setToolActivityVisible(
+			this.#toolActivityVisible && (!toolName || !this.#hiddenToolActivityNames.has(toolName)),
+		);
+	}
+
 	override addChild(component: Component): void {
-		if (isToolActivityComponent(component)) component.setToolActivityVisible(this.#toolActivityVisible);
+		this.#applyToolActivityVisibility(component);
 		super.addChild(component);
 		this.#entries.push({
 			component,
@@ -186,9 +195,20 @@ export class TranscriptContainer extends Container {
 	setToolActivityVisible(visible: boolean): void {
 		if (this.#toolActivityVisible === visible) return;
 		this.#toolActivityVisible = visible;
-		for (const child of this.children) {
-			if (isToolActivityComponent(child)) child.setToolActivityVisible(visible);
+		for (const child of this.children) this.#applyToolActivityVisibility(child);
+		this.invalidate();
+	}
+
+	setHiddenToolActivityNames(names: readonly string[]): void {
+		const normalized = new Set(names.map(name => name.trim().toLowerCase()).filter(Boolean));
+		if (
+			normalized.size === this.#hiddenToolActivityNames.size &&
+			[...normalized].every(name => this.#hiddenToolActivityNames.has(name))
+		) {
+			return;
 		}
+		this.#hiddenToolActivityNames = normalized;
+		for (const child of this.children) this.#applyToolActivityVisibility(child);
 		this.invalidate();
 	}
 
diff --git a/packages/coding-agent/src/modes/controllers/event-controller.ts b/packages/coding-agent/src/modes/controllers/event-controller.ts
index 0913d9f33ed8a3b7db732ca44df86c782d5eedf0..1c97651f9e0219f65121c7386058e3e8d7e27abe 100644
--- a/packages/coding-agent/src/modes/controllers/event-controller.ts
+++ b/packages/coding-agent/src/modes/controllers/event-controller.ts
@@ -997,7 +997,9 @@ export class EventController {
 			this.ctx.streamingMessage = event.message;
 			this.ctx.streamingComponent.pickReactionTarget(this.ctx.chatContainer.children);
 			this.ctx.chatContainer.addChild(this.ctx.streamingComponent);
-			const timeline = splitAssistantMessageToolTimeline(this.ctx.streamingMessage);
+			const timeline = splitAssistantMessageToolTimeline(this.ctx.streamingMessage, {
+				hideToolPreamble: settings.get("display.hideAssistantToolPreambles"),
+			});
 			this.#streamingReveal.begin(this.ctx.streamingComponent, timeline.beforeTools, timeline.hasToolCalls);
 			this.ctx.ui.requestRender();
 		}
@@ -1196,7 +1198,9 @@ export class EventController {
 				this.#streamingReveal.resyncVisibility();
 			}
 			this.ctx.streamingMessage = event.message;
-			const timeline = splitAssistantMessageToolTimeline(this.ctx.streamingMessage);
+			const timeline = splitAssistantMessageToolTimeline(this.ctx.streamingMessage, {
+				hideToolPreamble: settings.get("display.hideAssistantToolPreambles"),
+			});
 			this.#streamingReveal.setTarget(timeline.beforeTools, timeline.hasToolCalls);
 
 			const visibleBlockCount = this.ctx.streamingMessage.content.filter(
@@ -1401,7 +1405,9 @@ export class EventController {
 						stopReason: "stop",
 					}
 				: this.ctx.streamingMessage;
-			const displayTimeline = splitAssistantMessageToolTimeline(displayMessage);
+			const displayTimeline = splitAssistantMessageToolTimeline(displayMessage, {
+				hideToolPreamble: settings.get("display.hideAssistantToolPreambles"),
+			});
 			const linkTargets = await refreshAssistantMessageLinkTargets(this.ctx, [displayMessage]);
 			if (!this.ctx.streamingComponent.isTranscriptBlockFinalized()) {
 				this.ctx.streamingComponent.setLinkTargets(
diff --git a/packages/coding-agent/src/modes/controllers/extension-ui-controller.ts b/packages/coding-agent/src/modes/controllers/extension-ui-controller.ts
index 3111a0047f18a11314672cf60e41f77ba823b087..985c4442cafa736c4c2ba609cf8cf668fe05fe1b 100644
--- a/packages/coding-agent/src/modes/controllers/extension-ui-controller.ts
+++ b/packages/coding-agent/src/modes/controllers/extension-ui-controller.ts
@@ -20,6 +20,7 @@ import type {
 	ExtensionWidgetOptions,
 	SendUserMessageHandler,
 	TerminalInputHandler,
+	TranscriptVisibility,
 } from "../../extensibility/extensions";
 import { getSessionSlashCommands } from "../../extensibility/extensions/get-commands-handler";
 import { AskDialogComponent, boundPromptTitle } from "../../modes/components/ask-dialog";
@@ -64,6 +65,40 @@ function toWireSelectOptions(options: ExtensionUISelectItem[]): CollabUiSelectIt
 	);
 }
 
+function normalizeTranscriptVisibility(value: unknown): TranscriptVisibility | undefined {
+	if (typeof value !== "object" || value === null || Array.isArray(value)) return undefined;
+	const candidate = value as Partial<TranscriptVisibility>;
+	if (
+		typeof candidate.hideThinking !== "boolean" ||
+		typeof candidate.hideToolActivity !== "boolean" ||
+		typeof candidate.hideAssistantToolPreambles !== "boolean" ||
+		typeof candidate.hideTokenUsage !== "boolean" ||
+		!Array.isArray(candidate.hiddenTools) ||
+		!candidate.hiddenTools.every(name => typeof name === "string")
+	) {
+		return undefined;
+	}
+	return {
+		hideThinking: candidate.hideThinking,
+		hideToolActivity: candidate.hideToolActivity,
+		hideAssistantToolPreambles: candidate.hideAssistantToolPreambles,
+		hideTokenUsage: candidate.hideTokenUsage,
+		hiddenTools: [...new Set(candidate.hiddenTools.map(name => name.trim().toLowerCase()).filter(Boolean))].sort(),
+	};
+}
+
+function normalizeTranscriptVisibilityProfiles(value: unknown): Record<string, TranscriptVisibility> {
+	if (typeof value !== "object" || value === null || Array.isArray(value)) return {};
+	const profiles: Record<string, TranscriptVisibility> = {};
+	for (const [rawName, rawProfile] of Object.entries(value)) {
+		const name = rawName.trim().replace(/\s+/g, " ");
+		const profile = normalizeTranscriptVisibility(rawProfile);
+		if (!name || name.length > 64 || /[\x00-\x1f\x7f]/.test(name) || !profile) continue;
+		profiles[name] = profile;
+	}
+	return profiles;
+}
+
 export class ExtensionUiController {
 	#extensionTerminalInputUnsubscribers = new Set<() => void>();
 	#composerShapeDisposers: Array<() => void> = [];
@@ -142,6 +177,38 @@ export class ExtensionUiController {
 			setEditorComponent: factory => this.ctx.setEditorComponent(factory),
 			getToolsExpanded: () => this.ctx.toolOutputExpanded,
 			setToolsExpanded: expanded => this.ctx.setToolsExpanded(expanded),
+			getTranscriptVisibility: () => ({
+				hideThinking: this.ctx.hideThinkingBlock,
+				hideToolActivity: this.ctx.hideToolActivity,
+				hideAssistantToolPreambles: this.ctx.settings.get("display.hideAssistantToolPreambles"),
+				hideTokenUsage: !this.ctx.settings.get("display.showTokenUsage"),
+				hiddenTools: [...this.ctx.settings.get("display.hiddenTools")],
+			}),
+			setTranscriptVisibility: (visibility: TranscriptVisibility) => {
+				const hiddenTools = [
+					...new Set(visibility.hiddenTools.map(name => name.trim().toLowerCase()).filter(Boolean)),
+				].sort();
+				this.ctx.hideThinkingBlock = visibility.hideThinking;
+				this.ctx.hideToolActivity = visibility.hideToolActivity;
+				this.ctx.settings.set("hideThinkingBlock", visibility.hideThinking);
+				this.ctx.settings.set("display.hideToolActivity", visibility.hideToolActivity);
+				this.ctx.settings.set("display.hideAssistantToolPreambles", visibility.hideAssistantToolPreambles);
+				this.ctx.settings.set("display.showTokenUsage", !visibility.hideTokenUsage);
+				this.ctx.settings.set("display.hiddenTools", hiddenTools);
+				this.ctx.chatContainer.setToolActivityVisible(!visibility.hideToolActivity);
+				this.ctx.chatContainer.setHiddenToolActivityNames(hiddenTools);
+				this.ctx.rebuildChatFromMessages();
+				if (visibility.hideToolActivity || hiddenTools.length > 0) this.ctx.ui.clearInlineImages();
+				this.ctx.ui.resetDisplay();
+			},
+			getTranscriptVisibilityProfiles: () =>
+				normalizeTranscriptVisibilityProfiles(this.ctx.settings.get("display.transcriptVisibilityProfiles")),
+			setTranscriptVisibilityProfiles: profiles => {
+				this.ctx.settings.set(
+					"display.transcriptVisibilityProfiles",
+					normalizeTranscriptVisibilityProfiles(profiles),
+				);
+			},
 		};
 		this.ctx.setToolUIContext(uiContext, true);
 		this.#toolUIContext = uiContext;
diff --git a/packages/coding-agent/src/modes/interactive-mode.ts b/packages/coding-agent/src/modes/interactive-mode.ts
index 536c6c816b3cad8bf75be98e036463ed09aa4ad7..93c590caf3baceaf0fb70197f255a3d4c74260fa 100644
--- a/packages/coding-agent/src/modes/interactive-mode.ts
+++ b/packages/coding-agent/src/modes/interactive-mode.ts
@@ -1001,6 +1001,7 @@ export class InteractiveMode implements InteractiveModeContext {
 
 		this.hideToolActivity = settings.get("display.hideToolActivity");
 		this.chatContainer.setToolActivityVisible(!this.hideToolActivity);
+		this.chatContainer.setHiddenToolActivityNames(settings.get("display.hiddenTools"));
 		this.hideThinkingBlock = settings.get("hideThinkingBlock");
 		this.proseOnlyThinking = settings.get("proseOnlyThinking");
 
diff --git a/packages/coding-agent/src/modes/utils/transcript-render-helpers.ts b/packages/coding-agent/src/modes/utils/transcript-render-helpers.ts
index d3584e22099db9e4ee39d65c80f53eb5ba0b0ace..4bb6e6e520d186f26b006d4b726b7cecdfd3220f 100644
--- a/packages/coding-agent/src/modes/utils/transcript-render-helpers.ts
+++ b/packages/coding-agent/src/modes/utils/transcript-render-helpers.ts
@@ -192,7 +192,10 @@ export function assistantHasVisibleContent(message: AssistantAgentMessage): bool
  * in one assistant message; keeping every text block in the leading assistant
  * block buries post-tool text above tool results in the transcript.
  */
-export function splitAssistantMessageToolTimeline(message: AssistantAgentMessage): {
+export function splitAssistantMessageToolTimeline(
+	message: AssistantAgentMessage,
+	options: { hideToolPreamble?: boolean } = {},
+): {
 	beforeTools: AssistantAgentMessage;
 	afterToolCalls: ReadonlyMap<string, AssistantAgentMessage>;
 	hasToolCalls: boolean;
@@ -236,7 +239,10 @@ export function splitAssistantMessageToolTimeline(message: AssistantAgentMessage
 		return { beforeTools: message, afterToolCalls, hasToolCalls: false };
 	}
 
-	return { beforeTools: displaySegment(beforeTools), afterToolCalls, hasToolCalls: true };
+	const visibleBeforeTools = options.hideToolPreamble
+		? beforeTools.filter(content => content.type !== "text")
+		: beforeTools;
+	return { beforeTools: displaySegment(visibleBeforeTools), afterToolCalls, hasToolCalls: true };
 }
 
 /**
diff --git a/packages/coding-agent/src/modes/utils/ui-helpers.ts b/packages/coding-agent/src/modes/utils/ui-helpers.ts
index 61e62cba7974a085113cc91e2f959824e2ce5d15..faa7605825ad23b7e47e4e62bbc933463995f7d2 100644
--- a/packages/coding-agent/src/modes/utils/ui-helpers.ts
+++ b/packages/coding-agent/src/modes/utils/ui-helpers.ts
@@ -915,6 +915,7 @@ export class UiHelpers {
 		const visibleChatContainer = this.ctx.chatContainer;
 		const stagedChatContainer = new TranscriptContainer();
 		stagedChatContainer.setToolActivityVisible(!this.ctx.hideToolActivity);
+		stagedChatContainer.setHiddenToolActivityNames(this.ctx.settings.get("display.hiddenTools"));
 		const preservedChatChildren = options.preserveExistingChat ? [...visibleChatContainer.children] : undefined;
 		const previousTranscriptMessageComponents = this.ctx.transcriptMessageComponents;
 		const previousPendingTools = this.ctx.pendingTools;
diff --git a/packages/coding-agent/src/session/session-maintenance.ts b/packages/coding-agent/src/session/session-maintenance.ts
index a9f67b3815507c111f3fce6f71dd107387f1eecf..fcfa6b9a9b6eeba4ff03c11c4566e4f777bc180d 100644
--- a/packages/coding-agent/src/session/session-maintenance.ts
+++ b/packages/coding-agent/src/session/session-maintenance.ts
@@ -1580,6 +1580,24 @@ export class SessionMaintenance {
 		);
 	}
 
+	#assertProviderDispatchFitsContextWindow(contextWindow: number, messages: AgentMessage[]): void {
+		const opts = { excludeEncryptedReasoning: true } as const;
+		const outboundTokens =
+			computeNonMessageTokens(this.#host.nonMessageTokenSource(), this.#tokenizer) +
+			this.#tokenizer.countMessages(messages, opts);
+		if (outboundTokens <= contextWindow) return;
+		throw new Error(
+			`Provider request blocked before dispatch: ${outboundTokens} estimated input tokens exceed the ${contextWindow}-token context window after synchronous compaction.`,
+		);
+	}
+	#assertPendingProviderDispatchFitsContextWindow(contextWindow: number, pendingMessages: AgentMessage[]): void {
+		const outboundTokens = this.#estimateStoredContextTokens(pendingMessages);
+		if (outboundTokens <= contextWindow) return;
+		throw new Error(
+			`Provider request blocked before dispatch: ${outboundTokens} estimated input tokens exceed the ${contextWindow}-token context window after synchronous compaction.`,
+		);
+	}
+
 	#estimatePrePromptContextTokens(messages: AgentMessage[], contextWindow: number): number {
 		const breakdown = this.#host.getContextBreakdown({ contextWindow, pendingMessages: messages });
 		const localEstimate = this.#estimateStoredContextTokens(messages);
@@ -1624,11 +1642,12 @@ export class SessionMaintenance {
 		if (!model) return;
 		const contextWindow = model.contextWindow ?? 0;
 		if (contextWindow <= 0) return;
+		const providerRequestLimit = Math.min(contextWindow, model.cost.longContext?.inputThreshold ?? contextWindow);
 		const compactionSettings = this.#host.settings.getGroup("compaction");
 		const contextTokens = this.#estimatePrePromptContextTokens(messages, contextWindow);
 		const pendingMidTurnDeadEnd = this.#midTurnDeadEndPendingPrePrompt;
 		this.#midTurnDeadEndPendingPrePrompt = false;
-		if (!shouldCompact(contextTokens, contextWindow, compactionSettings)) {
+		if (!shouldCompact(contextTokens, contextWindow, compactionSettings) && contextTokens <= providerRequestLimit) {
 			this.maybeStartSpeculativeCompaction(contextTokens, contextWindow);
 			return;
 		}
@@ -1640,12 +1659,16 @@ export class SessionMaintenance {
 			// The prior tool loop already attempted the rescue and warned for this
 			// persisted oversized turn. Only a later persisted cut point makes a
 			// pre-prompt retry useful; the new agent loop may warn for its own turn.
+			this.#assertPendingProviderDispatchFitsContextWindow(providerRequestLimit, messages);
 			return;
 		}
 		// Grace band: a live (or just-started) background speculation absorbs the
 		// blocking summarization; the user's prompt goes out immediately and the
 		// armed result is spliced in at the next boundary.
-		if (this.deferThresholdCompactionToSpeculation(contextTokens, contextWindow)) {
+		if (
+			contextTokens <= providerRequestLimit &&
+			this.deferThresholdCompactionToSpeculation(contextTokens, contextWindow)
+		) {
 			logger.debug("Pre-prompt threshold deferred to speculative compaction", {
 				contextTokens,
 				contextWindow,
@@ -1678,6 +1701,7 @@ export class SessionMaintenance {
 			preparedContextTokens: this.#estimateStoredContextTokens(),
 			phase: "pre_turn",
 		});
+		this.#assertPendingProviderDispatchFitsContextWindow(providerRequestLimit, messages);
 	}
 
 	/**
@@ -1706,6 +1730,7 @@ export class SessionMaintenance {
 		const model = this.#model;
 		const contextWindow = model?.contextWindow ?? 0;
 		if (contextWindow <= 0) return;
+		const providerRequestLimit = Math.min(contextWindow, model?.cost.longContext?.inputThreshold ?? contextWindow);
 
 		const compactionSettings = this.#host.settings.getGroup("compaction");
 		if (
@@ -1713,6 +1738,7 @@ export class SessionMaintenance {
 			!hasConfiguredCompactionMethod(compactionSettings) ||
 			compactionSettings.midTurnEnabled === false
 		) {
+			this.#assertProviderDispatchFitsContextWindow(providerRequestLimit, activeMessages);
 			return;
 		}
 
@@ -1729,7 +1755,7 @@ export class SessionMaintenance {
 		const billedContextTokens = calculateContextTokens(lastAssistant.usage);
 		const storedContextTokens = this.#estimateStoredContextTokens();
 		const contextTokens = compactionContextTokens(billedContextTokens, storedContextTokens);
-		if (!shouldCompact(contextTokens, contextWindow, compactionSettings)) {
+		if (!shouldCompact(contextTokens, contextWindow, compactionSettings) && contextTokens <= providerRequestLimit) {
 			this.maybeStartSpeculativeCompaction(contextTokens, contextWindow);
 			return;
 		}
@@ -1760,6 +1786,7 @@ export class SessionMaintenance {
 				prepareCompaction(this.#host.sessionManager.getBranch(), compactionSettings, model, this.#tokenizer) ===
 					undefined
 			) {
+				this.#assertProviderDispatchFitsContextWindow(providerRequestLimit, activeMessages);
 				return;
 			}
 			this.#midTurnCompactionDeadEnds.delete(activeMessages);
@@ -1799,6 +1826,7 @@ export class SessionMaintenance {
 		if (compactedMessages !== activeMessages) {
 			activeMessages.splice(0, activeMessages.length, ...compactedMessages);
 		}
+		this.#assertProviderDispatchFitsContextWindow(providerRequestLimit, activeMessages);
 		logger.debug("Mid-run compaction ran between provider calls", {
 			contextTokens,
 			contextWindow,
diff --git a/packages/coding-agent/test/modes/components/tool-activity-visibility.test.ts b/packages/coding-agent/test/modes/components/tool-activity-visibility.test.ts
index f041efac50d732b89ff32e8a2e3c08a25f519dfe..502acbcec6d7434daa037f8a88a973698f6fd3cf 100644
--- a/packages/coding-agent/test/modes/components/tool-activity-visibility.test.ts
+++ b/packages/coding-agent/test/modes/components/tool-activity-visibility.test.ts
@@ -65,4 +65,41 @@ describe("tool activity visibility", () => {
 		wrapper.setExpanded(false);
 		expect(states).toEqual([true, false]);
 	});
+
+	it("filters named tools independently from global activity visibility", () => {
+		class NamedActivity extends Text {
+			#visible = true;
+			constructor(
+				text: string,
+				private readonly toolName: string,
+			) {
+				super(text, 1, 0);
+			}
+			getToolActivityName(): string {
+				return this.toolName;
+			}
+			setToolActivityVisible(visible: boolean): void {
+				this.#visible = visible;
+			}
+			override render(width: number): readonly string[] {
+				return this.#visible ? super.render(width) : [];
+			}
+		}
+
+		const transcript = new TranscriptContainer();
+		transcript.setHiddenToolActivityNames([" BASH "]);
+		transcript.addChild(new NamedActivity("bash output", "bash"));
+		transcript.addChild(new NamedActivity("read output", "read"));
+		expect(stripVTControlCharacters(transcript.render(120).join("\n"))).toContain("read output");
+		expect(stripVTControlCharacters(transcript.render(120).join("\n"))).not.toContain("bash output");
+
+		transcript.setToolActivityVisible(false);
+		expect(stripVTControlCharacters(transcript.render(120).join("\n"))).toBe("");
+		transcript.setToolActivityVisible(true);
+		expect(stripVTControlCharacters(transcript.render(120).join("\n"))).toContain("read output");
+		expect(stripVTControlCharacters(transcript.render(120).join("\n"))).not.toContain("bash output");
+
+		transcript.setHiddenToolActivityNames([]);
+		expect(stripVTControlCharacters(transcript.render(120).join("\n"))).toContain("bash output");
+	});
 });
diff --git a/packages/coding-agent/test/modes/controllers/extension-ui-controller.test.ts b/packages/coding-agent/test/modes/controllers/extension-ui-controller.test.ts
index 70e851270b1393a4a3d453efbbb7dd650a414307..02f7b42553a3266e7273e896866445b6a9737a43 100644
--- a/packages/coding-agent/test/modes/controllers/extension-ui-controller.test.ts
+++ b/packages/coding-agent/test/modes/controllers/extension-ui-controller.test.ts
@@ -25,6 +25,14 @@ function makeHarness() {
 	const requestRender = vi.fn();
 	const setFocus = vi.fn();
 	const addAutocompleteProvider = vi.fn();
+	const settingsValues = new Map<string, unknown>([
+		["display.showTokenUsage", true],
+		["display.hideAssistantToolPreambles", false],
+		["display.hiddenTools", []],
+		["display.transcriptVisibilityProfiles", {}],
+	]);
+	const settingsSet = vi.fn((key: string, value: unknown) => settingsValues.set(key, value));
+	const rebuildChatFromMessages = vi.fn();
 	const fakeHandle = {
 		hide: vi.fn(),
 		setHidden: vi.fn(),
@@ -39,12 +47,25 @@ function makeHarness() {
 			setFocus,
 			showOverlay,
 			terminal: { rows: 40 },
+			clearInlineImages: vi.fn(),
+			resetDisplay: vi.fn(),
 		},
 		editorContainer,
 		session: {
 			extensionRunner: undefined,
 			setUsageFallbackConfirmer: vi.fn(),
 		},
+		settings: {
+			get: (key: string) => settingsValues.get(key),
+			set: settingsSet,
+		},
+		hideThinkingBlock: false,
+		hideToolActivity: false,
+		chatContainer: {
+			setToolActivityVisible: vi.fn(),
+			setHiddenToolActivityNames: vi.fn(),
+		},
+		rebuildChatFromMessages,
 		setToolUIContext(context: ExtensionUIContext, hasUI: boolean): void {
 			expect(hasUI).toBe(true);
 			uiContext = context;
@@ -64,6 +85,9 @@ function makeHarness() {
 		showOverlay,
 		fakeHandle,
 		controller,
+		settingsValues,
+		settingsSet,
+		rebuildChatFromMessages,
 		async init(): Promise<ExtensionUIContext> {
 			await controller.initHooksAndCustomTools();
 			expect(uiContext).toBeDefined();
@@ -94,6 +118,26 @@ describe("ExtensionUiController editor UI", () => {
 		expect(harness.requestRender).toHaveBeenCalledTimes(1);
 	});
 
+	it("persists usage visibility and named profiles through the extension UI", async () => {
+		const harness = makeHarness();
+		const ui = await harness.init();
+		const hidden = {
+			hideThinking: true,
+			hideToolActivity: false,
+			hideAssistantToolPreambles: true,
+			hideTokenUsage: true,
+			hiddenTools: ["bash"],
+		};
+
+		expect(ui.getTranscriptVisibility?.().hideTokenUsage).toBe(false);
+		ui.setTranscriptVisibility?.(hidden);
+		expect(harness.settingsValues.get("display.showTokenUsage")).toBe(false);
+		expect(harness.rebuildChatFromMessages).toHaveBeenCalledTimes(1);
+
+		ui.setTranscriptVisibilityProfiles?.({ Quiet: hidden });
+		expect(ui.getTranscriptVisibilityProfiles?.()).toEqual({ Quiet: hidden });
+	});
+
 	it("keeps a populated prompt visible and routes input to it until the draft is cleared", async () => {
 		const harness = makeHarness();
 		harness.editor.setText("finish this wor");
diff --git a/packages/coding-agent/test/modes/utils/transcript-render-helpers.test.ts b/packages/coding-agent/test/modes/utils/transcript-render-helpers.test.ts
index 18337996ad29563e96a0e1da85b9e6a937ff0b36..92af5cfb647e3706f11c3509d998b4524505f58d 100644
--- a/packages/coding-agent/test/modes/utils/transcript-render-helpers.test.ts
+++ b/packages/coding-agent/test/modes/utils/transcript-render-helpers.test.ts
@@ -1,6 +1,9 @@
 import { describe, expect, it } from "bun:test";
 import type { AssistantMessage, Usage } from "@oh-my-pi/pi-ai";
-import { assistantUsageIsBilled } from "../../../src/modes/utils/transcript-render-helpers";
+import {
+	assistantUsageIsBilled,
+	splitAssistantMessageToolTimeline,
+} from "../../../src/modes/utils/transcript-render-helpers";
 
 function usage(overrides: Partial<Usage> = {}): Usage {
 	return {
@@ -36,3 +39,34 @@ describe("assistantUsageIsBilled", () => {
 		expect(assistantUsageIsBilled(emptyFreeMessage.usage)).toBe(false);
 	});
 });
+
+describe("splitAssistantMessageToolTimeline", () => {
+	it("removes only pre-tool assistant text when requested", () => {
+		const message = {
+			role: "assistant",
+			content: [
+				{ type: "thinking", thinking: "private reasoning" },
+				{ type: "text", text: "**Checking files**" },
+				{ type: "toolCall", id: "call-1", name: "read", arguments: { path: "a.ts" } },
+				{ type: "text", text: "Final answer" },
+			],
+			stopReason: "toolUse",
+		} as AssistantMessage;
+
+		const visible = splitAssistantMessageToolTimeline(message);
+		expect(visible.beforeTools.content.map(content => content.type)).toEqual(["thinking", "text"]);
+
+		const filtered = splitAssistantMessageToolTimeline(message, { hideToolPreamble: true });
+		expect(filtered.beforeTools.content.map(content => content.type)).toEqual(["thinking"]);
+		expect(filtered.afterToolCalls.get("call-1")?.content).toEqual([{ type: "text", text: "Final answer" }]);
+	});
+
+	it("never hides text from a final assistant response without tools", () => {
+		const message = {
+			role: "assistant",
+			content: [{ type: "text", text: "Final answer" }],
+			stopReason: "stop",
+		} as AssistantMessage;
+		expect(splitAssistantMessageToolTimeline(message, { hideToolPreamble: true }).beforeTools).toBe(message);
+	});
+});
